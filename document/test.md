# 要求
现在前后端和相应的依赖都已跑通。为了验收，我需要进行最后一次联调。联调要求如下
1. 查看数据库能够反映出全部的数据状态。如果不行，则更换数据库中的测试数据（不要修改表创建数据！）。
2. 查看后端的test代码对后端生产代码的覆盖率是否足够，如果不足，则需要新增测试；如果和业务要求不符，则需要修改测试/业务代码。一切改动都要力求最小量的代码改动！
3. 前端与后端联调，若出现前端显示和后端代码不一致的问题，则需要查看原因并修改前端代码（由于后端在上一步已经联调过了，因此不需要再改动）。
4. 前端联调，查看是否出现数据和响应数据不一致等问题，如果有则修改前端代码。